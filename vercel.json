{
  "version": 2,
  "framework": null,
  "buildCommand": "tsc && vite build && cp -r public dist",
  "outputDirectory": "dist",
  "functions": {
    "api/index.js": {
      "includeFiles": "public/build/**"
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index.js" }
  ]
}
